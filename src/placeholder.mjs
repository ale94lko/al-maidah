/**
 * Placeholder module until the Nuxt application lands (MVP-01).
 * Gives CodeQL and the CI smoke tests a real JavaScript source file.
 */
export const projectName = "al-maidah";

export function getProjectName() {
  return projectName;
}
