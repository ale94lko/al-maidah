{
  "name": "al-maidah",
  "version": "0.1.0",
  "private": true,
  "description": "QR digital menu, kitchen ticket board, and owner panel for restaurants in the UAE.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/placeholder.mjs"
  }
}
